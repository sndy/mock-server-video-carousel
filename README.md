Mock server setup to fetch videoData from http://demo2697834.mockable.io/movies
CORS enabled

To run the server
==============================================
 - $> npm install
 - $> node app.js

 should see- Server listening on port 3000
